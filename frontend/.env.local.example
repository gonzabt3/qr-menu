# Backend API URL
# For local development, point to your Rails backend
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000

# Auth0 Configuration (if using Auth0 SDK directly)
# These may not be needed if using backend session-based auth
# NEXT_PUBLIC_AUTH0_DOMAIN=your-auth0-domain.auth0.com
# NEXT_PUBLIC_AUTH0_CLIENT_ID=your-client-id
# NEXT_PUBLIC_AUTH0_CALLBACK_URL=http://localhost:3001/api/auth/callback
